# CodeCoachAI V1 最终冒烟测试报告

## 1. 测试环境

- 测试时间：2026-05-14
- 工作区：`C:\my-claude`
- 前端仓库：`C:\my-claude\CodeCoachAI-vue`
- 后端仓库：`C:\my-claude\CodeCoachAI-java`
- 文档仓库：`C:\my-claude\CodeCoachAI-doc`
- 前端访问地址：`http://127.0.0.1:5173`
- Gateway 地址：`http://127.0.0.1:8080`
- 测试方式：浏览器冒烟验证 + Gateway 接口抽查 + 后端日志关键字检查。

## 2. 测试账号

| 角色 | username | password |
| --- | --- | --- |
| 管理员 | `e2e_admin` | `E2eAdmin@123` |
| 普通用户 | `e2e_user` | `E2eUser@123` |

## 3. 三个仓库当前状态

### CodeCoachAI-vue

- 当前分支：`dev`
- 工作区状态：未完全干净，存在未跟踪文件 `docs/V1剩余P0回归测试报告.md`
- 最近 3 条提交：
  - `59e597d Fix user tag filtering to use real tag IDs`
  - `d6a348d Refine V1 frontend contract and interview flows`
  - `b4ab76c Merge remote-tracking branch 'origin/dev' into dev`

### CodeCoachAI-java

- 当前分支：`dev`
- 工作区状态：未完全干净，存在已修改文件 `README.md`
- 最近 3 条提交：
  - `d6245ce Fix V1 question tags and report fallback`
  - `16289f0 Fix V1 backend filters and demo test data`
  - `c01f9ca chore: add nacos config and e2e test data`

### CodeCoachAI-doc

- 当前分支：`main`
- 工作区状态：未完全干净，存在未跟踪目录 `V1演示说明/`；本报告为本轮新增测试记录
- 最近 3 条提交：
  - `74aa242 Add V1 frontend integration interface checklist`
  - `b54c1a8 Refine V1 interface docs for internal calls`
  - `8d74e7c docs: add CodeCoachAI V1 design docs`

## 4. 基础服务检查结果

| 服务 | 检查结果 | 证据 |
| --- | --- | --- |
| Nacos | 通过 | `8848` 端口监听；服务列表返回 8 个服务 |
| MySQL | 通过 | `3306` 端口监听 |
| Redis | 通过 | `6379` 端口监听 |
| Gateway | 通过 | `8080` 端口监听 |
| 后端服务注册 | 通过 | Nacos 返回 `codecoachai-auth`、`codecoachai-question`、`codecoachai-gateway`、`codecoachai-interview`、`codecoachai-system`、`codecoachai-resume`、`codecoachai-user`、`codecoachai-ai` |
| 前端服务 | 通过 | `5173` 端口监听，浏览器可访问 |

## 5. 管理端冒烟测试结果

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| 管理员登录 | 通过 | 使用 `e2e_admin` 登录成功 |
| 进入管理端首页 | 通过 | `/admin` 页面正常展示 |
| 题目管理列表 | 通过 | `/admin/questions` 正常加载，列表有数据 |
| 题目标签筛选 | 通过 | 选择 `E2E_TEST_缓存` 后返回 `Total 3` |
| 题目状态筛选 | 通过 | 选择启用状态后返回启用题列表，未出现禁用题 |
| 分类管理 | 通过 | `/admin/question-categories` 正常加载 |
| 标签管理 | 通过 | `/admin/question-tags` 正常加载 |
| 问题组管理 | 通过 | `/admin/question-groups` 正常加载 |
| Prompt 模板 | 通过 | `/admin/ai/prompts` 正常加载 |
| 页面业务 404 | 通过 | 管理端冒烟流程未发现业务 404 |

## 6. 用户端冒烟测试结果

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| 普通用户登录 | 通过 | 使用 `e2e_user` 登录成功 |
| 用户首页 | 通过 | `/dashboard` 正常展示 |
| 题库页面 | 通过 | `/questions` 正常加载，默认 `Total 21` |
| 标签筛选 | 通过 | 选择 `E2E_TEST_缓存` 后返回 `Total 3`，不再出现 `Total 0` |
| 禁用题展示 | 通过 | 用户端题库未展示禁用题 |
| 题目详情 | 通过 | `/questions/100004` 正常打开，参考答案和解析正常展示 |
| 创建面试 | 通过 | `/interviews/create` 创建面试成功，本轮生成测试面试 `100007` |
| 面试房间未开始状态 | 通过 | `/interviews/room/100007` 显示 `NOT_STARTED` 和“开始面试”按钮，回答框与提交按钮禁用 |
| 开始面试 | 通过 | 点击“开始面试”后进入 `IN_PROGRESS`，当前问题展示，回答框和提交按钮可用 |
| 面试报告 | 通过 | `/interviews/100005/report` 正常打开 |
| 报告中文化 | 通过 | 报告核心内容为中文，包含 AI 总结、回答亮点、主要问题、复习建议 |
| Mock report 清除 | 通过 | 页面未出现 `Mock report` 或 `Mock report: the interview has been completed` |
| 总分来源说明 | 通过 | 页面可见“总分来源于后端面试报告 totalScore 字段” |
| 阶段得分空状态 | 通过 | 页面可见“暂无阶段得分数据” |
| V2 功能隐藏 | 通过 | 未展示学习计划、AI 简历优化等 V2 功能 |
| 页面业务 404 | 通过 | 用户端冒烟流程未发现业务 404 |

## 7. Network 异常请求检查结果

浏览器业务流程日志与后端日志关键字检查未发现以下异常：

- `/inner/**`
- 旧 `/ai/interview/**`
- `questions/answer`
- `questions/wrongs`
- 分类 / 标签 / 问题组 `/status` 子路径
- 用户端误调 `/admin/**`
- 业务 404
- `tagIds` 误传
- 英文 `Mock report`

异常请求清单：无。

## 8. 是否仍存在阻塞 V1 演示的问题

未发现仍阻塞 V1 演示的功能问题。

需要注意的是，三个仓库当前工作区并非完全干净。该问题不影响本次浏览器冒烟结论，但会影响封版标签的可追溯性。

## 9. 是否建议打 `v1-demo` 标签

建议打 `v1-demo` 标签，但建议先完成以下动作：

1. 确认并提交或明确保留 `CodeCoachAI-vue` 中未跟踪的 `docs/V1剩余P0回归测试报告.md`。
2. 确认并提交或还原 `CodeCoachAI-java` 中的 `README.md` 修改。
3. 确认并提交或明确保留 `CodeCoachAI-doc` 中的 `V1演示说明/` 目录和本报告。

不建议在工作区未处理干净的状态下直接打封版标签。

## 10. README 还需要补充的内容

- `CodeCoachAI-vue/README.md`：补充 V1 演示启动命令、`.env.development` 中 Gateway 地址、演示账号、关键演示路由。
- `CodeCoachAI-java/README.md`：补充基础依赖启动顺序、服务端口、Nacos 注册服务清单、`sql/dev_test_data.sql` 演示数据说明。
- `CodeCoachAI-doc/README.md` 或 `V1演示说明/`：补充 V1 演示边界、Network 禁用路径清单、冒烟测试步骤、封版打标签流程。

## 11. 下一步建议

1. 不继续修改业务代码，当前功能冒烟已经满足 V1 演示封版要求。
2. 先处理三个仓库未提交或未跟踪的文档/README 变更，保证封版标签可追溯。
3. 工作区确认后，在三个仓库分别打 `v1-demo` 标签。
4. 封版后只接受演示脚本、README、部署说明级别的文档补充；业务代码变更应进入后续版本分支。
